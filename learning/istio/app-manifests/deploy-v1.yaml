apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: first-app
    version: v1
  name: web-v1
  namespace: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: first-app
      version: v1
  template:
    metadata:
      labels:
        app: first-app
        version: v1
    spec:
      containers:
      - image: aputra/myapp-lesson155:latest
        name: first-app
        env:
        - name: SERVICE
          value: first-app
        - name: VERSION
          value: v1
        ports:
        - name: http
          containerPort: 8080
